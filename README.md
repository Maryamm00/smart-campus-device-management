# smart-campus-device-management
C++ command-line system for managing smart campus devices (security cameras, AC, projectors, lighting, door locks). Built with OOP and polymorphism via an abstract device base class, a Factory pattern for device creation, and smart pointers (unique_ptr) for automatic memory management — no manual delete calls.
